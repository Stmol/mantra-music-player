// Placeholder API notes until a component test runner is introduced.
// Modal supports bind:open, title, description, size, dismissible, onClose,
// children, and footer snippets.

export const modalApiContract = true;

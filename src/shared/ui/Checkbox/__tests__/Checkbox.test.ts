// Placeholder API notes until a component test runner is introduced.
// Checkbox supports native checkbox input attributes, bind:checked, label,
// description, indeterminate, and an optional marker snippet.

export const checkboxApiContract = true;

// Placeholder API notes until a component test runner is introduced.
// Button accepts native button attributes plus variant, size, loading, pressed,
// children, leading, and trailing snippets.

export const buttonApiContract = true;

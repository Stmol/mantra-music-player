// Placeholder API notes until a component test runner is introduced.
// Chip supports native button attributes plus create/removable behavior,
// fullWidth, and children content.

export const chipApiContract = true;

// Placeholder API notes until a component test runner is introduced.
// TextField supports native input attributes, bind:value, label, description,
// error, inputSize, tone, leading snippet, and trailing snippet.

export const textFieldApiContract = true;

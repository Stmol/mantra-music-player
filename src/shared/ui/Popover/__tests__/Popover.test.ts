// Placeholder API notes until a component test runner is introduced.
// Popover supports bind:open, placement, trigger snippet, and children snippet.
// The trigger and children snippets receive { open, toggle } render state.
// triggerMode="custom" lets callers render their own accessible trigger.

export const popoverApiContract = true;

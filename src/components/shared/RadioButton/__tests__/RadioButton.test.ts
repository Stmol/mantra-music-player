// Placeholder API notes until a component test runner is introduced.
// RadioButton supports native radio input attributes, bind:checked,
// bind:group, label, description, and an optional marker snippet.

export const radioButtonApiContract = true;

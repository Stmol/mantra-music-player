// Placeholder API notes until a component test runner is introduced.
// IconTabs supports bind:activeValue, icon-only items, disabled items, and a
// tablist role using Mantra segment-control and segment classes.

export const iconTabsApiContract = true;

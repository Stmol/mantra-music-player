// Placeholder API notes until a component test runner is introduced.
// Select supports native select attributes, bind:value, label, description,
// error, selectSize, and options.

export const selectApiContract = true;

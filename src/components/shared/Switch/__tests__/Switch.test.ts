// Placeholder API notes until a component test runner is introduced.
// Switch supports native button attributes, bind:checked, label, description,
// and optional onIcon/offIcon snippets.

export const switchApiContract = true;

// Placeholder API notes until a component test runner is introduced.
// ProgressMeter accepts value/max, clamps overflow and underflow, and applies a
// token-driven accent fill without changing layout metrics.

export const progressMeterApiContract = true;

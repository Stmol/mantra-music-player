// Placeholder API notes until a component test runner is introduced.
// NavList supports active rows, optional icons, optional trailing metadata, and
// forwards row selection through its callback prop.

export const navListApiContract = true;

// Placeholder API notes until a component test runner is introduced.
// StackedLinkList supports a titled header, optional header action, compact
// rows with trailing metadata, and optional row selection callbacks.

export const stackedLinkListApiContract = true;

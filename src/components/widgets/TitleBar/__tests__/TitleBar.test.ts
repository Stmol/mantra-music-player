// Placeholder API notes until a component test runner is introduced.
// TitleBar owns the unified application toolbar, reserves native traffic-light
// space on macOS, and renders custom window controls on Windows and Linux.

export const titleBarApiContract = true;
